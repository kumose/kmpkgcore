set(Z_BUILD_INFO_FILE_CONTENTS "CRTLinkage: ${KMPKG_CRT_LINKAGE}\n")
string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "LibraryLinkage: ${KMPKG_LIBRARY_LINKAGE}\n")
if (DEFINED KMPKG_POLICY_DLLS_WITHOUT_LIBS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyDLLsWithoutLIBs: ${KMPKG_POLICY_DLLS_WITHOUT_LIBS}\n")
endif()
if (DEFINED KMPKG_POLICY_DLLS_WITHOUT_EXPORTS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyDLLsWithoutExports: ${KMPKG_POLICY_DLLS_WITHOUT_EXPORTS}\n")
endif()
if (DEFINED KMPKG_POLICY_DLLS_IN_STATIC_LIBRARY)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyDLLsInStaticLibrary: ${KMPKG_POLICY_DLLS_IN_STATIC_LIBRARY}\n")
endif()
if (DEFINED KMPKG_POLICY_MISMATCHED_NUMBER_OF_BINARIES)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyMismatchedNumberOfBinaries: ${KMPKG_POLICY_MISMATCHED_NUMBER_OF_BINARIES}\n")
endif()
if (DEFINED KMPKG_POLICY_EMPTY_PACKAGE)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyEmptyPackage: ${KMPKG_POLICY_EMPTY_PACKAGE}\n")
endif()
if (DEFINED KMPKG_POLICY_ONLY_RELEASE_CRT)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyOnlyReleaseCRT: ${KMPKG_POLICY_ONLY_RELEASE_CRT}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_OBSOLETE_MSVCRT)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowObsoleteMsvcrt: ${KMPKG_POLICY_ALLOW_OBSOLETE_MSVCRT}\n")
endif()
if (DEFINED KMPKG_POLICY_EMPTY_INCLUDE_FOLDER)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyEmptyIncludeFolder: ${KMPKG_POLICY_EMPTY_INCLUDE_FOLDER}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_RESTRICTED_HEADERS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowRestrictedHeaders: ${KMPKG_POLICY_ALLOW_RESTRICTED_HEADERS}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_DUMPBIN_CHECKS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipDumpbinChecks: ${KMPKG_POLICY_SKIP_DUMPBIN_CHECKS}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_ARCHITECTURE_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipArchitectureCheck: ${KMPKG_POLICY_SKIP_ARCHITECTURE_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_CMAKE_HELPER_PORT)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyCmakeHelperPort: ${KMPKG_POLICY_CMAKE_HELPER_PORT}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_ABSOLUTE_PATHS_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipAbsolutePathsCheck: ${KMPKG_POLICY_SKIP_ABSOLUTE_PATHS_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_ALL_POST_BUILD_CHECKS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipAllPostBuildChecks: ${KMPKG_POLICY_SKIP_ALL_POST_BUILD_CHECKS}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_APPCONTAINER_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipAppcontainerCheck: ${KMPKG_POLICY_SKIP_APPCONTAINER_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_CRT_LINKAGE_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipCrtLinkageCheck: ${KMPKG_POLICY_SKIP_CRT_LINKAGE_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_MISPLACED_CMAKE_FILES_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipMisplacedCMakeFilesCheck: ${KMPKG_POLICY_SKIP_MISPLACED_CMAKE_FILES_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_LIB_CMAKE_MERGE_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipLibCMakeMergeCheck: ${KMPKG_POLICY_SKIP_LIB_CMAKE_MERGE_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_DLLS_IN_LIB)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowDllsInLib: ${KMPKG_POLICY_ALLOW_DLLS_IN_LIB}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_MISPLACED_REGULAR_FILES_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipMisplacedRegularFilesCheck: ${KMPKG_POLICY_SKIP_MISPLACED_REGULAR_FILES_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_COPYRIGHT_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipCopyrightCheck: ${KMPKG_POLICY_SKIP_COPYRIGHT_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_KERNEL32_FROM_XBOX)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowKernel32FromXBox: ${KMPKG_POLICY_ALLOW_KERNEL32_FROM_XBOX}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_EXES_IN_BIN)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowExesInBin: ${KMPKG_POLICY_ALLOW_EXES_IN_BIN}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_USAGE_INSTALL_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipUsageInstallCheck: ${KMPKG_POLICY_SKIP_USAGE_INSTALL_CHECK}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_EMPTY_FOLDERS)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowEmptyFolders: ${KMPKG_POLICY_ALLOW_EMPTY_FOLDERS}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_DEBUG_INCLUDE)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowDebugInclude: ${KMPKG_POLICY_ALLOW_DEBUG_INCLUDE}\n")
endif()
if (DEFINED KMPKG_POLICY_ALLOW_DEBUG_SHARE)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicyAllowDebugShare: ${KMPKG_POLICY_ALLOW_DEBUG_SHARE}\n")
endif()
if (DEFINED KMPKG_POLICY_SKIP_PKGCONFIG_CHECK)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "PolicySkipPkgConfigCheck: ${KMPKG_POLICY_SKIP_PKGCONFIG_CHECK}\n")
endif()

if (DEFINED KMPKG_HEAD_VERSION)
    string(APPEND "Z_BUILD_INFO_FILE_CONTENTS" "Version: ${KMPKG_HEAD_VERSION}\n")
endif()

file(WRITE "${CURRENT_PACKAGES_DIR}/BUILD_INFO" "${Z_BUILD_INFO_FILE_CONTENTS}")
