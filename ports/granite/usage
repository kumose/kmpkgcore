The package granite provides CMake integration:

    # link shared library
    find_package(granite CONFIG REQUIRED)
    target_link_libraries(main PRIVATE granite::granite_shared)

    # or link static library

    target_link_libraries(main PRIVATE granite::granite_static)
